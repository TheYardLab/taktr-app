// firestore.rules
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMP: full read/write for everyone
    match /{document=**} {
      allow read, write: if true;
    }
  }
}